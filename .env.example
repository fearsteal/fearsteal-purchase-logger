# Keep actual .env secrets on your backend host only; do not commit them.
PORT=3000
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/REPLACE_WITH_ID/REPLACE_WITH_TOKEN
PURCHASE_LOG_SECRET=replace-with-a-long-random-secret
